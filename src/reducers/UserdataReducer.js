const initialstate = "";
const updateUserData = (state = initialstate, action) => {
    switch (action.type) {
        case "INSERTUSERDATA":
            d
            break;
        case "UPDATEUSERDATA":

            break;
        default:
            break;
    }
}